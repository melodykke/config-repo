
zuul:
  routes:
#    myProduct:
#      path: /myProduct/**
#      serviceId: product
    product: /product/**
  ignored-patterns:
       - /**/pppproduct/listForOrder
management:
  endpoints:
    web:
      exposure:
        include: "*"
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
